<%= form_for(@hitchhike) do |f| %>
  <% if @hitchhike.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hitchhike.errors.count, "error") %> prohibited this hitchhike from being saved:</h2>

      <ul>
      <% @hitchhike.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
